---
sudo: required
language: python
services:
  - docker
install:
  - pip install molecule
  - pip install docker
  - pip install docker-py
env:
branches:
  only:
    - master

script:
  # run molecule tests
  - molecule --debug test
notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/
